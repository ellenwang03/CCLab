// when night player needs to stop or mirror shatters, else if player reaches mirror, createCapture(VIDEO);

function setup() {
  let canvas = createCanvas(800, 500);
  canvas.parent("canvasContainer");
}

function draw() {
  
}

